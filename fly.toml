# Placeholder. The real app name is passed at deploy time with --app, from FLY_APP locally or
# the FLY_APP_NAME secret in CI, so this file stays the same in every fork.
# Staging uses fly.stg.toml (separate app and volume).
app = "your-app-name"
primary_region = "ams"

[build]

# Server env for Bun (not Vite): merchant routes need MERCHANT_DID == store owner did (same as VITE_MERCHANT_DID).
#   fly secrets set MERCHANT_DID=did:plc:…
[env]
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  cpus = 2
  cpu_kind = "shared"
  memory = "2048mb"

# Create once, with the name below: fly volumes create bazaar_data --region ams --size 1 -a <your-app>
# Volumes are scoped to an app, so this is a different volume from the staging one that shares
# its name -- there is no way for the two to collide.
[[mounts]]
  source = "bazaar_data"
  destination = "/data"
