{
  "lexicon": 1,
  "id": "diamonds.whereditgo.bazaar.catalog.listing",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": [
          "item",
          "price",
          "status",
          "licenseGrant",
          "createdAt"
        ],
        "properties": {
          "item": {
            "$ref": "diamonds.whereditgo.bazaar.defs#ref"
          },
          "price": {
            "$ref": "diamonds.whereditgo.bazaar.defs#money"
          },
          "status": {
            "type": "string",
            "knownValues": [
              "active",
              "paused",
              "soldOut",
              "archived",
              "superseded"
            ]
          },
          "licenseGrant": {
            "$ref": "diamonds.whereditgo.bazaar.defs#ref",
            "description": "The license.terms record governing this listing, pinned at listing creation. Required. Different listings for the same item may reference different license.terms records, enabling per-tier pricing (e.g. personal vs. commercial). cid anchors the offered terms so that receipt resolution is consistent even if the license.terms record is later updated."
          },
          "parentListing": {
            "type": "string",
            "format": "at-uri",
            "description": "When this listing sells a member catalog.item individually, the AT-URI of the parent catalog.listing for the product it belongs to. Absent for standalone items and for the product listing itself. Used for storefront grouping and cascade pause when the product listing is paused."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
