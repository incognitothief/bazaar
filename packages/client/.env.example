# Exposed to the browser via import.meta.env.VITE_*
VITE_ATPROTO_SERVICE=https://bsky.social
VITE_APP_DID=did:plc:PLACEHOLDER
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_PLACEHOLDER
VITE_APP_URL=http://localhost:5173
VITE_LEXICON_NAMESPACE=diamonds.whereditgo.bazaar
VITE_ARTIST_DID=did:plc:PLACEHOLDER
# Bun API origin for browser calls. Leave empty to use same origin (Vite /api proxy or SPA on API host).
# If set (e.g. http://localhost:3000), the server must allow your SPA origin in CORS_ALLOWED_ORIGINS.
VITE_API_ORIGIN=
